import { action, computed, observable } from 'mobx'
import BaseStore from './BaseStore'

class ProfileImageStore extends BaseStore {
	constructor () {
		super()
	}
}


export default new ProfileImageStore()
